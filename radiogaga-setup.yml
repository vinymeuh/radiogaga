---
- name: Setting up radiogaga
  hosts: radiogaga
  roles:
    - { role: ssh, tags: ['never', 'ssh'] }
    - { role: base, tags: ['base'] }
    - { role: usbkey, tags: ['usbkey'] }
  tasks:
    - name: Commit changes
      command: lbu commit -d
      tags: always
